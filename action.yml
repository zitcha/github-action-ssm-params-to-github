name: SSM Params to Github
author: Sam Anthony

runs:
  using: "composite"
  steps:
  - name: Import SSM Params
    shell: bash
    run: |
      ls -la
      cd ${{ github.action_path }}/scripts
      composer install
      php run.php $GITHUB_OUTPUT
